services:
  dashboard:
    build: .
    ports:
      - "3014:3014"
    env_file:
      - .env
    volumes:
      - ./service-account.json:/app/service-account.json:ro
      - ./reports:/app/reports
    restart: unless-stopped
